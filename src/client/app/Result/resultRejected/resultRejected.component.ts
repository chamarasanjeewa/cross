import { Component,Input } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'result-rejected',
  templateUrl: 'resultRejected.component.html',
  styleUrls: ['resultRejected.component.css']
})

export class ResultRejectedComponent {
    // @Input() item: any;
     areaExpanded:boolean=false;
     constructor(){
       
     }

 }
